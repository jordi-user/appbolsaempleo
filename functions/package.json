{
  "name": "appbolsaempleo-notifications",
  "version": "1.0.0",
  "description": "Cloud Functions para notificaciones de la app de bolsa de empleo",
  "main": "src/index.js",
  "scripts": {
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "engines": {
    "node": "20"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^4.5.0"
  }
}